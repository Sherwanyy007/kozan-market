{
  "name": "kozan-market",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}